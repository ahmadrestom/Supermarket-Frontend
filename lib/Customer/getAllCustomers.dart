import 'package:flutter/material.dart';

class GetAllCustomers extends StatelessWidget{
  const GetAllCustomers({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
        appBar: AppBar(
        title: const Text('Get All Customers Here'),
      ),
    );
    }
}